# Boxes

| box | provider | region | plan / specs | runs-what | $/mo | provisioned-by | backups (last tested restore) | status |
|-----|----------|--------|--------------|-----------|------|----------------|-------------------------------|--------|
| syd1.swordfish.cfd | Vultr | syd (Sydney) | `vhf-1c-2gb` — 1 vCPU / 2 GB / 64 GB NVMe / 2 TB bw | Swordfish tooling (Dokploy · Kuma · Beszel · hello); Project 2 from Stage 2 | 12 | `provisioning/cloud-init/syd1.yaml` + `provisioning/vultr/create-box.ps1` (2026-07-07, founder-approved gate); edge/control plane via `edge-apply`; backup layer via `backups-apply` | restic→B2 nightly 15:00 UTC (`swordfish-syd1-backups`); **restore drill 2026-07-07** runner-side, RTO 3 s / RPO ≤24 h (`runbooks/backup-restore.md`); Bucket-4: kuma+beszel SQLite dumps in the set, dead-man dual ping (healthchecks + kuma push) | **live** — 45.63.24.122, hardened at first boot, `hardening-smoke` = the standing verification. Bucket-2 host layer 2026-07-07: provider firewall `swordfish-syd1` (22/80/443 only), daemon.json log caps (no live-restore — swarm-incompatible), reboot window 18:30 UTC, swap 6.2G + swappiness 10, sshd crypto floor (ssh-audit [fail]-clean). Bucket-2 edge 2026-07-07: swordfish-traefik v3.6.7 + socket-proxy own 80/443, Dokploy v0.29.10 pinned (no 3000 publish), TLS live at `deploy.swordfish.cfd` (LE, TLS-ALPN) — 39/39 assertions + CI green-lock probe green. Bucket-4 dogfood 2026-07-07: Kuma 2.4.0 at `status.` + Beszel 0.18.7 at `metrics.` deployed via Dokploy MCP (`runbooks/dogfood.md`), socket-proxy v0.4.2 fleet-wide, ratelimit on public routers (deploy. exempt), 60/60 assertions; hello at `hello.` pending GHCR PAT; RAM watch: ~71% used with stacks idle (Bucket-5 resize input) |

*(purchase approved + executed 2026-07-07 at the Bucket-1 gate; access = CI-as-hands via the `swordfish-ci` key (repo secret `SSH_DEPLOY_KEY`) + founder break-glass key; box billing started — $12/mo against the $250 credit, within the $30/mo Stage-1–2 ceiling)*
