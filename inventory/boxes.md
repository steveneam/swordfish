# Boxes

| box | provider | region | plan / specs | runs-what | $/mo | provisioned-by | backups (last tested restore) | status |
|-----|----------|--------|--------------|-----------|------|----------------|-------------------------------|--------|
| syd1.swordfish.cfd | Vultr | syd (Sydney) | `vhf-1c-2gb` — 1 vCPU / 2 GB / 64 GB NVMe / 2 TB bw | Swordfish tooling (Dokploy · Kuma · Beszel · hello); Project 2 from Stage 2 | 12 | `provisioning/cloud-init/syd1.yaml` + `provisioning/vultr/create-box.ps1` (2026-07-07, founder-approved gate); edge/control plane via `edge-apply`; backup layer via `backups-apply` | restic→B2 nightly 15:00 UTC (`swordfish-syd1-backups`); **restore drill 2026-07-07** runner-side, RTO 3 s / RPO ≤24 h (`runbooks/backup-restore.md`); Bucket-4: kuma+beszel SQLite dumps in the set, dead-man dual ping (healthchecks + kuma push) | **live** — 45.63.24.122, hardened at first boot, `hardening-smoke` = the standing verification. Bucket-2 host layer 2026-07-07: provider firewall `swordfish-syd1` (22/80/443 only), daemon.json log caps (no live-restore — swarm-incompatible), reboot window 18:30 UTC, swap 6.2G + swappiness 10, sshd crypto floor (ssh-audit [fail]-clean). Bucket-2 edge 2026-07-07: swordfish-traefik v3.6.7 + socket-proxy own 80/443, Dokploy v0.29.10 pinned (no 3000 publish), TLS live at `deploy.swordfish.cfd` (LE, TLS-ALPN) — 39/39 assertions + CI green-lock probe green. Bucket-4 dogfood 2026-07-07: Kuma 2.4.0 at `status.` + Beszel 0.18.7 at `metrics.` deployed via Dokploy MCP (`runbooks/dogfood.md`), socket-proxy v0.4.2 fleet-wide, ratelimit on public routers (deploy. exempt), 60/60 assertions; hello live at `hello.` (GHCR pull via founder PAT in Dokploy); ntfy + UptimeRobot alert legs verified; RAM watch: ~71% used with stacks idle (Bucket-5 resize input). **Bucket-4 CHECKPOINT 2026-07-08: Stage 1 COMPLETE — box graduated**; Dokploy-MCP verdict KEEP; Bucket-5 path = graduate-by-migrating (BinaryLane SYD 8 GB) with parallel-run — this box stays live until the successor passes full verification, then the instance is destroyed (Vultr account + credit retained as fallback provider) |

| syd2.swordfish.cfd | BinaryLane | syd (Sydney) | `std-4vcpu` — 4 vCPU / 8 GB / 100 GB NVMe / 4 TB bw | Stage-2 shared box (successor to syd1 via parallel-run); Thalon web app = first tenant post-verification, Thalon render offload second | AUD 39.20 (≈US$26) | `provisioning/cloud-init/syd2.yaml` pasted into the purchase form (founder-executed Bucket-5 gate, 2026-07-08 — API token only exists post-purchase); rebuild path = `provisioning/binarylane/create-box.ps1` (no-op converge proven against the live box) | restic→B2 nightly 15:00 UTC (`swordfish-syd2-backups`, scoped key); **first backup + restore drill 2026-07-08** runner-side, RTO 3 s / RPO 0 h, content-verified (run 28885492767); dogfood dump hooks self-arming (kuma/beszel SKIP until deployed); dead-man receivers deliberately NOT installed yet (parallel-run — syd2 gets its own at the replay leg) | **edge + control plane live** — 103.249.236.41 (id 638535), Ubuntu 24.04, hardened at first boot: first smoke 2026-07-08 (run 28883030398) = all Phase 0–2 assertions green. **Swap datapoint: BinaryLane image ships NO swapfile** — the cloud-init converge guard created the 2G `/swapfile` (Vultr's image had shipped 6.2G). Provider firewall applied 2026-07-08 via `provisioning/binarylane/set-firewall.ps1` (accept tcp 22/80/443 → drop all; idempotent no-op re-run proven); A-records `syd2` + temp `deploy2` → box IP (Porkbun, scripted). Bucket-5 edge 2026-07-08: `edge-apply` vs syd2 (run 28883853825) — swordfish-traefik + socket-proxy own 80/443, Dokploy v0.29.10 pinned at `deploy2.swordfish.cfd` (parallel-run temp name; `DEPLOY_FQDN` parameterized, cutover re-point is the same converge re-run), idempotency proven in CI, real LE cert issued (laptop CA-validated probe: HTTP 307 = fresh Dokploy awaiting /register). Bucket-5 backups 2026-07-08: layer converged + idempotent (run 28885238961), **UTC clock catch** — BinaryLane image ships Australia/Sydney so every schedule resolved local; now pinned Etc/UTC at cloud-init/phase2/assertion layers, timers re-elapsed (run 28885433026). **Posture 55/63** — remaining FAILs = dogfood (7) + kuma-url (1), the replay leg. Dokploy admin registered by founder at deploy2; agent API key in laptop `.env`. Bucket-5 dogfood replay 2026-07-08 via Dokploy REST (project swordfish: status composeId LozRPFJ8LCLK88hvc7ke5 · metrics 1PiSAb1dg6TiRGw9X1C4Q · hello applicationId -a2FMiW1gq30KstVf37w6 appName swordfish-hello-ksv6id): kuma healthy + beszel hub/agent up + hello running, domains carry real + temp names (status2/metrics2 real LE certs; ratelimit on all public routers), kuma/beszel bootstraps converged (syd2 push monitor, ntfy to founder phone, syd2 system record), dump hooks armed (kuma+beszel dumps in snapshot), **both dead-man legs receiver-acknowledged from syd2** (healthchecks `swordfish-syd2-backups` + syd2's own Kuma push, run 28886561884). **Posture 60/63 = pre-cutover ceiling** (run 28886809947; 3 FAILs are sniStrict route checks for real names whose certs can only issue post-cutover). Next: verify-deadman natural fire (15:00 UTC timer) → cutover gate → 63/63 → soak → destroy Vultr instance |

| syd3.swordfish.cfd | Vultr | syd (Sydney) | `vhf-1c-2gb` — 1 vCPU / 2 GB / 64 GB NVMe / 2 TB bw | **Agent cockpit** (founder terminal over SSH + Claude Code CLI; machine-migration call 2026-07-10) — cockpit-class: NO workloads, NO Docker, port 22 only | 12 (credit-funded) | `provisioning/cloud-init/syd3.yaml` + `provisioning/vultr/create-box.ps1 -Label syd3` (2026-07-10, founder-approved gate); provider firewall `swordfish-syd3` (tcp/22 only) via `firewall-group.ps1`; A-record `syd3` → box IP (Porkbun, scripted) | restic→B2 nightly 15:00 UTC (`swordfish-syd3-backups`, scoped key `swordfish-syd3-restic`; source = `/home/deploy` whole-home, standalone profile — see the inherit-merges-positionally lesson in `provisioning/backup/profiles.yaml`); **first backup + restore drill 2026-07-10** runner-side (apply run 29091998789, drill run 29092092325), RTO 3 s / RPO 0 h, content-verified; dead-man off-infra leg LIVE 2026-07-10 (founder-created healthchecks check; URL in `.env` + secret `SYD3_HEALTHCHECKS_PING_URL`; receiver-acked ping on apply run 29092668496, snapshot 24a9101c); Kuma push leg = post-cutover follow-up | **live** — 139.180.170.11 (id 779ceedf-bb34-44da-b5dd-cc98f91383f3), Ubuntu 24.04, hardened at first boot per syd3.yaml (fleet sshd/ufw/fail2ban baseline; dev toolchain git·gh·tmux·Node22·Claude Code baked in); `cockpit-smoke` = the standing verification (fleet `hardening-smoke` does not apply by design — no Docker/edge) |

*(syd1: purchase approved + executed 2026-07-07 at the Bucket-1 gate; box billing started — $12/mo against the $250 credit. syd2: purchase founder-executed 2026-07-08 at the Bucket-5 gate — AUD $39.20/mo cash, bounded two-box overlap pre-acknowledged at the Bucket-4 checkpoint until cutover verifies. Access to both = CI-as-hands via the `swordfish-ci` key (repo secret `SSH_DEPLOY_KEY`) + founder break-glass key)*
