# inventory/ — the fleet ledger

`boxes.md` holds one row per live box. **Secrets never live here** — `inventory/secrets/` is gitignored, and credentials/account IDs are recorded outside the repo (founder-held). Workloads are named by their anonymized labels (Project 1/2/3, Walter services, Swordfish tooling).
