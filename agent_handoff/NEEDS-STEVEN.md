# NEEDS STEVEN — the founder action queue

> Agent-maintained. One line per OPEN founder action:
> `- [YYYY-MM-DD] text` (date = when it was raised; the dashboard shows age).
> Remove a line ONLY when its source says done — the dashboard renders this
> file verbatim and never invents or retires state on its own.
> (Naming: the portfolio is fully unmasked as of 2026-07-17 — Eamos · Selom ·
> Thalon may all be named here. No guarded tokens remain.)

- [2026-07-17→19] 🔴 **STILL BLOCKED — GitHub Actions billing (re-verified 2026-07-19 01:37 UTC: dispatch 29668888819 refused before any runner started, same "recent account payments have failed or your spending limit needs to be increased" annotation).** Two workflows did run green mid-day 07-18, so something briefly cleared — but the block is back. **Tonight it mattered: syd2's edge was DOWN post-reboot and the CI channels (edge-apply / backups-apply) could not run; swordfish repaired over direct SSH instead (see the accounted note at the bottom).** Fix: GitHub → Settings → Billing & plans → clear the failed payment / raise the Actions limit. Original impact list still applies: Between **09:04 and 09:36 UTC** every GitHub Actions job across ALL repos began failing **before any runner started** (`steps=0`, nothing executed). Verified independently from the job annotations, not relayed: _"The job was not started because recent account payments have failed or your spending limit needs to be increased."_ Evidence: swordfish `ci-guard` **09:04 SUCCESS → 09:36/09:43/09:52 FAILURE**; thalon 09:47/09:51 FAILURE; eamos 09:47 FAILURE. **Fix:** GitHub → Settings → **Billing & plans** → clear the failed payment and/or raise the Actions spending limit (private repos meter Actions minutes; public ones are free). **What it is blocking:** ① **Eamos's migration is stopped dead** — their Compose contract is pushed and locally green (163 web tests + full backend suite + prod build) but they correctly refuse to deploy from a red commit, so Compose → cutover → soak → **Render cancel** all wait on this; ② **swordfish's CI-as-hands is down** (edge-apply, backups-apply, hardening-smoke, project1-apply cannot run); ③ thalon's CI is down; ④ swordfish's last 3 commits are pushed but CI-unverified — the guard ran locally and PASSED on each, so content is clean, only the required check is red. **Still working without CI:** box-side SSH work (the B2 purge/reseed runs restic on the box) and the nightly backup timer.
- [2026-07-17] **Eamos Phase 3: GATE ISSUED by you (08:10Z, their session) — nothing needed from you until Phase 4.** Peak-disk proven green from their code (no resize needed); they are freezing the exact 45-object landing contract, then seeding (~41 GB, zero spend) → cutover → parallel-run soak. **Your next action is Phase 4: cancel Render (−US$40/mo) after ① checksums green ② the frozen contract fully landed ③ soak clean** — swordfish will put it back on this list when all three are true.
- [2026-07-17] ⏳ **RENDER CANCEL — eamos's soak evidence is strong; their verdict, then yours.** **Your traffic is LIVE on syd2** and Render is idle-but-live as rollback. **The 503 that held it is now well characterised — and it is NOT the box and NOT the migration:** eamos's authoritative tally is **viewer 18× 200 / 2× 422 / just 2× 503 / 0× 429, zero other 5xx, zero tracebacks, zero OOM**. Both 503s were in the first 133 s after container start; their best-supported cause is a **transient external upstream** (VariantValidator coordinate-resolution timeouts — corroborated by concurrent `TimeoutError`/`ReadTimeout` warnings that still returned 200 via the app's fallback). Infra clean throughout: no restart, no OOM, no cgroup pressure, no traefik error, memory ~57% of cap. **They added their own ratchet** (`--require-ok` audit now exits nonzero and preserves the structured error if any future 5xx recurs). ⚠️ **Correction on swordfish's earlier report to you:** the "mysterious third 503 at 11:57" **never existed** — swordfish's grep matched `503` inside a log timestamp's nanoseconds; eamos caught it. There were only ever 2. **Nothing is blocked on swordfish.** When eamos declares the soak clean → tell them directly to cancel Render (−US$40/mo).
- [2026-07-13→17] **SPEND GATE — resize syd2. ⚠️ RECOMMENDATION AS OF 2026-07-17: DON'T SPEND YET — and it does NOT block cancelling Render.** Thalon MEASURED the render spike today (`VmHWM` during a real 50.8 s 1080×1080 x264 render), retiring the old "3–4 GB" guess: **ffmpeg worker = 2.26 GiB**, app+render concurrent = 4.09 GiB. Against syd2 measured the same day (5.90 GiB available; `thalon-web` = 76 MiB against a 4 GiB *cap* — a limit, not a reservation): **even after Eamos's backend cuts over (~2 GiB), ONE render worker still fits with ~1.6 GiB to spare.** Only a SECOND overlapping render would OOM. Thalon's own verdict: renders are operator-triggered, minutes long, and the queue-of-one fallback is **costing them nothing today**. So the resize buys *concurrency headroom you are not yet using* — the trigger is a future condition (renders moving onto syd2 AND overlapping, or Eamos's post-cutover memory eating the margin), not a present pain. If/when you do take it: `std-6vcpu` = 6 vCPU / 16 GB / 180 GB = **AUD 78.40/mo (+39.20)** priced 07-16 — **re-price live at the gate, never from memory** — and it needs a **power-off** (Dokploy + thalon + tenant-pg briefly down → peer-coordinated window) with the disk growing 100→180 GB **one-way, cannot shrink**.
- [2026-07-17→19] ✅ _(accounted, no action)_ **SYD4 16 GB RESIZE — FIRED on your in-session "go syd4 16gb" and LANDED 2026-07-19 02:19 UTC.** syd4 is now **`std-6vcpu`: 16 GiB RAM / 6 vCPU, AUD 78.40/mo (+39.20 over the old 39.20)** — verified on the box (`free` 15Gi, `nproc` 6) and via the API (server reports std-6vcpu/16384/180, status active). One ~5 s power-cycle at 02:19:48; every unit came back (agent-tmux, thalon's postgres, code-server, relay, dashboard), both swap files active, syd2 edge probed green from here. Thalon told in their channel (their docs assumed 8 GB). **COMPLETE incl. disk as of 02:27 UTC:** a second provider reboot presented the 180 GB disk and the filesystem grew to 177G on its own (no manual growpart needed); API confirms the resize action finished (status active, nothing in-progress). All units re-verified after the second reboot, syd2 edge re-probed green. Nothing left to watch. _Original context (for the record):_ 07-18's two attempts were refused host-capacity; the 07-19 ~02:00 re-probe showed capacity and you gave the in-session go after two 07-18 attempts were refused host-capacity. syd2's DON'T-SPEND stands unchanged (line above).
- [2026-07-17] _(accounted, no action)_ **eamos deploy key RELEASED by you 2026-07-17** — their CI wiring is complete (`DOKPLOY_API_BASE` var + `DOKPLOY_API_KEY` + `DOKPLOY_APP_ID` secrets in `steveneam/eamos-dev`). The key was **dead when released** (empty `accessedServices` — minted 07-15, their app born 07-16); swordfish granted its one service and proved it by consuming: reads its own service HTTP 200, denied thalon's HTTP 401, sees project1 only. Push→auto-deploy still deliberately NOT wired until they move to Dokploy Compose (an Application redeploy can silently strip read-only-root/cap-drop — automating that is automating the regression). Nothing needed from you.
- [2026-07-17] **POSTURE QUESTION (nothing broken, no rush): the shared Dokploy control plane means swordfish's admin API key can read EVERY tenant's live secrets in plaintext.** Surfaced while answering an eamos infra question today — their application record carries its `env` inline, so reading the record to check its deploy config also returns their Supabase **service-role** key, a JWT secret, an AI-gateway key, a DB URL with inline password, and your GitHub PAT (Dokploy's registry auth). **No value was reproduced anywhere** — not in chat, files, channels, or git. This is inherent to a shared Dokploy, not a defect: the control plane must hold env to inject it, and the admin key is the god key by design. But it sits underneath the charter's separation-of-duties claim ("swordfish does not reach into their codebase") — architecturally, swordfish *can* read their crown jewels any time it queries the control plane. Options, cheapest first: **(a) accept + record it** (it is the honest state of a shared control plane; tenants should know); **(b)** swordfish's own MCP switches to a **scoped tenant key** for tenant reads where one exists (`inventory/secrets/dokploy-tenant-*.env` already follows this pattern) and keeps the admin key for fleet ops only; **(c)** per-tenant Dokploy orgs — real isolation, real overhead. Eamos has been told, in the same note that answered their question. Your call, whenever.
- [2026-07-16] **POSTURE RULING wanted (no rush, nothing broken): syd4 can now SSH straight into syd2.** Disabling BinaryLane's outbound port-blocking on syd4 (your approval, for Render access) revealed that syd4's key was *always* authorized on syd2 — the "CI-as-hands only" property was accidentally enforced by the provider filter, not by policy. Nothing on syd2 was weakened; syd4 merely gained egress. Effect: agents on the workspace box can now administer production directly, bypassing the CI channel your charter chose. It is convenient (today's read-only probes used it) but it is a real reach expansion. **Keep** (say nothing) **or close** (drop that key from syd2's `authorized_keys`; probes move back to CI — costs a few minutes per check, no capability lost).
- [2026-07-19] ⚠️ **KEY ROTATION CALL (no rush, nothing left the box): swordfish's own provider keys hit a session transcript.** A malformed `.env` (values separated from keys by a space — bash runs `KEY= value` as a command) made sourcing it echo the VALUES of essentially every swordfish-held provider credential into the 07-19 session transcript as error text: BinaryLane, Porkbun (both), Vultr, both Dokploy admin keys, B2 backup keys, the GHCR pull PAT, UptimeRobot, and a healthchecks ping URL. On-box only (syd4, deploy-owned transcript files), never in git/channels — the same exposure class as the 07-18 thalon pair, which was ruled "rotate by the book." The file format is fixed (can't recur via that path). **Your call, ranked by blast radius if you want rotations: ① Porkbun (DNS control) ② BinaryLane + both Dokploy keys (infra/control-plane god keys) ③ B2 (backups) + GHCR PAT ④ Vultr ⑤ UptimeRobot/ping URL (trivial).** Each rotation is a swordfish-run pass once you say go; they can be spread over days.
- [2026-07-13] subscriptions.yml FILL fields — 3 remain in `inventory/secrets/subscriptions.yml` (edit the file or tell any agent): Claude → which card; GitHub → next_charge date + which card (from the receipt).
- [2026-07-13] Gmail MCP re-auth (token EXPIRED) — one click in claude.ai settings; unblocks receipt-driven subscriptions.yml fills.

_Accounted 2026-07-19 (incident, RESOLVED, no action needed): **syd2's public edge was down ~7h15m (18:30 UTC 07-18 → 01:46 07-19)** — the fleet kernel-patch reboot raced traefik against swarm-overlay init, the loss hard-stopped its restart policy, and Dokploy's 23:50 docker cleanup pruned the stopped container + image; with CI billing-blocked nothing could recreate it. All workloads (thalon staging, eamos containers, Dokploy, status pages) were healthy the whole time — only ingress was missing. Restored 01:46; ratcheted with a boot-time systemd unit (`swordfish-edge-up.service`) that waits for the overlay and re-runs compose, asserted in hardening checks. Same night the syd2 nightly-backup failure (a latent dump-hook validation bug + the retired thalon PGlite hook) was found and fixed; off-box snapshots resumed 01:40 UTC and healthchecks is green. Open follow-up on swordfish's side: why kuma never alerted you during 7h of edge-down._

_Accounted 2026-07-15 (informational lines, read + acted): Dokploy bookmark
(deploy.swordfish.cfd, in use since cutover) · thalon staging one-login note
(creds in `~/COPY-ME.txt`). Render-cancel guardrail folded into the spend-gate
line above — it is Phase 4 of the migration plan, not a separate action._

_Ruled 2026-07-16 (raised + decided same session, no longer needs him):
**render-only content** — Project 1's agent proved 10 objects (5.06 GB) live
only on the Render disk with no identity match in the source bucket (the
nothing-lives-only-on-Render check firing exactly as designed, early, while
Render is alive). Founder ruled **exact-byte preservation**: upload that set
(and only that set) to the bucket, rerun the comparator to `render_only=0`,
then the ClinGen dry run proceeds. No spend (~5 GB against 57 GB free
storage). Phase-4 blocker retired rather than carried; the alternative —
editing a passing safety test to accept the gap — was declined. Egress
readout that unblocked the gate: Pro, 25 Jun–25 Jul cycle, 0.014 of 250 GB
used._
