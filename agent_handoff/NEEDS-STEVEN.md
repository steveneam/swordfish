# NEEDS STEVEN — the founder action queue

> Agent-maintained. One line per OPEN founder action:
> `- [YYYY-MM-DD] text` (date = when it was raised; the dashboard shows age).
> Remove a line ONLY when its source says done — the dashboard renders this
> file verbatim and never invents or retires state on its own.
> ⛔ Guarded project names never go here (tracked file — Project 1/2 only).

- [2026-07-13] One-time pass: correct `inventory/secrets/subscriptions.yml` — Claude plan price + next-charge dates are placeholders (billing APIs approved and live as of this session).
- [2026-07-11] Confirm the migration rehearsal passed — this un-parks the ops queue (cutover step-card → verify-deadman → soak/syd1 → Thalon wiring → E1).
- [2026-07-13] Drive: the 9 loose .txt resume prompts are consolidated into each project's handoff — you are cleared to delete them from the drive.
- [2026-07-13] Census re-scan on the Mac (`mac-census.sh`) at the end of the cockpit-build session — the final nothing-left-behind check before the drive retires.
- [2026-07-13] Hermes has TERMINAL access on syd3 (it ran nslookup when you posted "ping-e1") — E0 posture said terminal disabled. Say "disable it" or "leave it" (E1 plan = manual approvals + deny rules).
- [2026-07-13] E1 relay E2E: the **Swordfish-topic leg PASSED live** (your "How you doing?" arrived relay-prefixed; the reply landed back in the topic). Remaining: a plain message in the **thalon** topic — the thalon agent's reply should land back there. Kill switch: `!kill` in the topic; poller backstop: `sudo systemctl stop swordfish-relay`.
- [2026-07-13] thalon staging: /blog is FIXED and there is now ONE login for everything (site + workspace) — creds refreshed in `~/COPY-ME.txt`; the old second (steven:…) workspace password is retired.
